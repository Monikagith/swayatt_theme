[project]
name = "swayatt_theme"
authors = [
    {name = "Swayatt", email = "sunny@swayatt.com"}
]
description = "Custom Theme for ERPNext"
requires-python = ">=3.11"
dynamic = ["version"]

dependencies = []

[build-system]
requires = ["flit_core>=3.4,<4"]
build-backend = "flit_core.buildapi"
